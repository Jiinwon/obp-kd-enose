"""Evaluation utilities for sensor drift scenarios."""
from __future__ import annotations


def split_by_time(series):
    """Placeholder time-based split function."""
    mid = len(series) // 2
    return series[:mid], series[mid:]
